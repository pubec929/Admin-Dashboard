{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./src/app/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "nodemon",
    "esbuild": "esbuild app.ts --bundle --servedir=www --outdir=www/js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "esbuild": "^0.19.2",
    "esbuild-node-tsc": "^2.0.5",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
